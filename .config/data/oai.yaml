_target_: data.oai.build
data_dir: oai/v00/data/inputs
anns_dir: meniscaltears/data
binary: true
multilabel: true # AH, B, PH
limit_train_items: false # will take up to N examples
limit_val_items: false # will take up to N examples
limit_test_items: false # will take up to N examples
